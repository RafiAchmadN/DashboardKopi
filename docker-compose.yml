services:
  kopi-dashboard:
    build: .
    container_name: kopi-dashboard
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - web

networks:
  web:
    external: true
